<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ system splash configuration -->
    <style name="Theme.BeerReal.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/beer_amber_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.BeerReal</item>
    </style>
</resources>

